spring:
  application:
    name: codesentry
  ai:
    model:
      chat: google-genai
    google:
      genai:
        api-key: ${GEMINI_API_KEY}
        chat:
          model: gemini-3.5-flash-lite
logging:
  level:
    org.springframework.ai: DEBUG
